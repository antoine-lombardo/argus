{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "expo",
      "request": "attach",
      "projectRoot": "${workspaceFolder}",
      "bundlerHost": "127.0.0.1",
      "bundlerPort": 8081,
      "preLaunchTask": "Run"
    },
    {
      "name": "Debug (plugin store)",
      "type": "expo",
      "request": "attach",
      "projectRoot": "${workspaceFolder}",
      "bundlerHost": "127.0.0.1",
      "bundlerPort": 8081,
      "preLaunchTask": "Run (plugin store)"
    }
  ]
}
